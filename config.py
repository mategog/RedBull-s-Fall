"""Project configuration: scope (teams, seasons) and storage paths.

Keep this Spark-free so notebooks, tests, and the Streamlit app can import it.

TODO: define SEASONS, TEAMS, and a layer_path(layer, table) helper that
builds paths under DATA_ROOT (read from the environment).
"""
